# Binaries
bin/
*.exe
*.test
*.out

# Go workspace
vendor/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Config with secrets
shirakami.yaml
shirakami.local.yaml

# Runtime data
diffs/
reports/
pwd.txt
repos_dedup.txt
MULTI_REPO_ANALYSIS.md
extract_repos.py
k8s/
scripts/tapd_analyze.py

# Diff / patch files (loose patches in repo root; golden case patches are tracked separately)
/diffs/*.patch
/reports/*.patch

# Golden test cases — private cases (based on internal repos) are NOT committed.
# Only open-source cases (go-*, py-*, cross-grpc-*) are tracked.
# Add private case dirs below to prevent accidental commit:
tests/golden/cases/compute-*/
tests/golden/cases/cross-repo-dispatch/
tests/golden/cases/vstation-*/
tests/golden/cases/cvm-*/
tests/golden/cases/vsresource-*/

# Coverage
coverage.out
coverage.html
