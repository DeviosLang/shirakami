diff --git a/compute/common/utils.py b/compute/common/utils.py
index abc1234..def5678 100644
--- a/compute/common/utils.py
+++ b/compute/common/utils.py
@@ -165,7 +165,12 @@ def execute(cmd, timeout=300):
     """Execute a shell command with timeout.

     Args:
         cmd: command string or list
         timeout: timeout in seconds (default 300)
+
+    Returns:
+        tuple: (return_code, stdout, stderr)
+
+    Raises:
+        TimeoutError: if command exceeds timeout
     """
-    return subprocess.run(cmd, shell=True, timeout=timeout, capture_output=True)
+    try:
+        result = subprocess.run(cmd, shell=True, timeout=timeout, capture_output=True, text=True)
+        return result.returncode, result.stdout, result.stderr
+    except subprocess.TimeoutExpired as e:
+        raise TimeoutError("Command timed out after %ds: %s" % (timeout, cmd)) from e
